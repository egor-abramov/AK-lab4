# Лабораторная работа №4
* Абрамов Егор P3219
* `forth | risc | neum | mc | tick | binary | stream | mem | pstr | prob1 | superscalar`

## Язык крограммирования
``` ebnf
<program> ::= <term> | <term> <program>
<term> ::= <procedure_def> | <var_def> | <instruction>

<instruction> ::= <math_op>
	            | <logic_op>
	            | <stack_op>
                | <addr_op>
	            | <io_op>
                | <loop_exp>
                | <cond_exp>
                | <number>
                | <string_literal>
                | <identifier>
                | <execution_token>
                | <execute_op>

<instruction_list> ::= <instruction> | <instruction> <instruction_list>

<math_op> ::= "+" | "-" | "*"

<logic_op> ::= "and" | "not" 	

<stack_op> ::= "dup" | "drop" | "swap"

<addr_op> ::= "!" 		          
	        | "@" 		                  

<io_op> ::= "read" 		        
	      | "." 	
	      | read_str
	      | print_str	        

<loop_exp> ::= "loop" <instruction_list> "endloop" 					                

<cond_exp> ::= "=0" <instruction>  
	         | ">0" <instruction>	  
	         
<string_literal> ::= '"' string '"'

<identifier> ::= [a-zA-Z0-9_]*

<var_def> ::= "var" <identifier> 
            | "array" <identifier>
            | "string" <string_literal> <identifier>

<procedure_def> ::= ":" <identifier> <instruction_list> ";"

<execution_token> ::= "'" <identifier>

<execute_op> ::= "execute"
```

Операции:

- `+` - сложить два верхних значения на стеке, результат положить на вершину
- `-` - вычесть из второго значения на стеке верхнее, результат положить на вершину
- `*` - перемножить два верхних значения на стеке, результат положить на вершину
- `and` - выполнить побитовое «И» для двух верхних значений на стеке
- `not` - выполнить логическое отрицание верхнего значения на стеке
- `dup` - продублировать верхнее значение на стеке
- `drop` - удалить верхнее значение со стека
- `swap` - поменять местами два верхних значения на стеке
- `!` - сохранить значение с вершины стека по адресу, равному второму элементу в стеке
- `@` - загрузить на стек значение по адресу с вершины стека. 
- `var <name>` - выделить машинное слово под переменную `<name>`
- `array <name>` - создать массив, количество слов в котором равно числу на стеке
- `string "..." <name>"` - сохранить строку в память и связать ее адрес с `<name>`
- `read` - ввести числовое значение из потока ввода и положить на стек
- `.` - напечатать числовое значение с вершины стека
- `read_str` - считывать символы из потока ввода в память по адресу с вершины стека, пока не встретится перенос строки
- `write_str` - напечатать строку в поток вывода, адрес которой находится на вершине стека
- `loop ... endloop` - начать выполнение цикла, тело которого ограничено `endloop`. Цикл заканчивается, если после выполнения тела цикла на стеке лежит 0. 
- `=0` - если значение на вершине стека равно 0, выполнить следующую за этим инструкцию
- `>0` - если значение на вершине стека больше 0, выполнить следующую за этим инструкцию
- `' <name>` - положить на стек Execution Token слова
- `execute` - взять Execution Token с вершины стека и перейти к выполнению кода по этому адресу
- `<name>` - положить на стек адрес переменной/процедуры `<name>`

Команды выполняются последовательно. 
Видимость данных - глобальная. 
Стековая модель вычислений.

На уровне стека все данные рассматриваются как числа. Одно и то же значение может быть интерпретироваться как число или как ASCII-символ, в зависимости от интсрукции.
Поддерживаются числовые и строковые литералы (```var```, `string`)

## Организация памяти
Память представляет из себя единое адресное пространство из 2048 ячеек. Машинное слово - 32 бита. 

Memory-mapped I/O
#### Варианты адресации
- Непосредственная загрузка 
- Абсолютная 
- Косвенная
- Регистровая

#### Доступные регистры
- `SP` - указатель на стек данных
- `RP` - Указатель на стек возвратов
- `Zero` - всегда хранит 0
- `X0-X4` - регистры общего назначения
- `A0-A3` - регистры для передачи аргументов / возврата значений из функции
- `T0-T3` - регистры для промежуточных операций

#### Разделение памяти
| Ячейки        | Назначение      |
|---------------|-----------------|
| 0x0 - 0x5F7   | Основная память | 
| 0x5F8         | Поток ввода     | 
| 0x5FC         | Поток вывода    | 
| 0x600 - 0x6FF | Стек данных     | 
| 0x700 - 0x7FF | Стек возвратов  | 

Статические данные размещаются в основной памяти сразу после завершения сегмента исполняемого кода (после `HALT`).
Строки храняться в формате *Pascal-строк*. Каждый элемент строки занимает машинное слово *целиком* (4 байта). Символы хранятся последовательно
## Система команд
#### Кодирование инструкций:
* Инструкции имеют фиксированный размер в 4 байта. 
* Первые 5 бит инструкции - код операции. 
* Регистры кодируются 4 битами.
* Адресация кодируется 3 битами

#### Набор инструкций
* Load Upper Immediate
  * ``lui <rd>, <k>``
  * Выделяет младшие 20 бит `k`, побитово сдвигает на 12 влево и загружает в регистр `rd`
  * Операция: `rd <- (k & 0x000FFFFF) << 12`
* Move 
  * `mv <rd>, <rs>`
  * Перемещает значение из регистра `rs` в регистр `rd`
  * Операция: `rd <- rs`
* Store
  * `sw <rs2>, <offset>(<rs1>)`
  * Сохраняет значение из регистра `rs2` по адресу, вычисляемому как сумма значения регистра `rs1` и `offset`
  * Операция: `Mem[rs1 + offset] <- rs2` 
* Load 
  * `lw <rd>, <offset>(<rs>)`
  * Загружает значение в регистр `rd` из адреса, вычисляемого как сумма  значения регистра `rs` и `offset`
  *  Операция: `rd <- Mem[rs + offset]`
* Add Immediate
  * `addi <rd>, <rs>, <k>`
  * Прибавляет к значению в регистре `rs` младшие 12 бит `k`, рвсширяя знак `k`. Результат сохраняется в регистр `rd`
  * Операция: `rd <- rs + signext(k & 0x00000FFF)` 
* Add 
  * `add <rd>, <rs1>, <rs2>`
  * Складывает значения регистров `rs1` и `rs2`. Результат сохраняется в регистр `rd`.
  * Операция: `rd <- rs1 + rs2`
* Substract 
  * `sub <rd>, <rs1>, <rs2>`
  * Вычитает значение регистра `rs2` из регистра `rs1`. Результат сохраняется в регистр `rd`.
  * Операция: `rd <- rs1 - rs2` 
* Multiply
  * `mul <rd>, <rs1>, <rs2>`
  * Умножает регистр `rs1` на `rs2`. Результат сохраняется в `rd`
  * Операция `rd <- rs1 * rs2` 
* Bitwise AND
  * `and <rd>, <rs1>, <rs2>`
  * Берет побитовое И регистров `rs1`и `rs2`. Результат сохраняется в `rd`.
  * Операция: `rd <- rs1 & rs2`
* Inversion
  * `inv <rd>, <rs>`
  * Битовая инверсия регистра `rs`. Результат сохраняется в `rd`
  * Операция `rd <- ~rs` 
* Jump
  * `j <k>`
  * Переход на адрес `k`
  * Операция: `PC <- k`
* Jump Register
  * `jr <rs>`
  * Переход на адрес в регистре `rs`
  * Операция: `PC <- rs`
* Jump If Zero Register
  * `jz <rs>, <k>`
  * Переход на адрес `k`, если в регистре `rs` записан ноль
  * Операция: `if rs == 0 then PC <- k `
* Halt
  * `halt`
  * Остановка 

## Транслятор
Интерфейс командной строки: `translator.py <input_file> <target_file>`

Транслятор на выходе формирует два файла: бинарный код и `json` файл со структурированным списком команд. `json` предназначен исключительно для отладки и не используется эмулятором.
В `json` файле данные представлены в следующем виде:
```
[
  {
          "address": 0,
          "type": "instruction" / "data",
          "label": "",
  
          "value": 0,                       <- для ячееек с данными
  
          "opcode": isa.Opcode,             <- для ячеек с инструкциями
          "args": [],
  }
]
```
Трансляция выполняется в *два прохода*
1. Вычисление размеров сегмента кода. Для выделения памяти под данные (`var`, `array`, `string`)
2. Генерация шитого кода

Транслятор переводит `forth` в прямой шитый код. То есть тело программы будет состоять из адресов подпрограмм, которые необходимо выполнить. Для исполнения шитого кода поверх процессора реализуется виртуальная `forth` машина.

Виртуальная машина использует регистры следующим образом:
  - `X0` - Instruction Pointer
  - `X1-X3` - регистры общего назначения
  - `SP` - Data Stack Pointer
  - `RP` - Retirn Stack Pointer

В модуле `translator` содержтся файл `kernel.s`, являющийся ядром виртуальной машины. Ядро состоит из двух частей:
1. Интерпретатор - обеспечиват переход между словами в шитом коде. Состоит из трех подрограмм:
   - `NEXT`. Забирает следующий адрес по указателю `X0` и переходит на него
   - `DOCOL`. Обработчик выхова процедур пользователя. Сохраняет текущий `X0` на стек возвратов и устанавливает `X0` на первый адрес вызываемой процедуры.
   - `EXIT`. Обработчик завершения процедуры. Восстанавливает со стека состояние `X0`.
2. Примитивы - реализация базовых слов (`+`, `-`, `DUP` и т.д)

## Модель процессора

## Тестирование
